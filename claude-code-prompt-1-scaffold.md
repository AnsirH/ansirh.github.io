# 작업: 포트폴리오 사이트 초기 구조 및 라우팅 스캐폴딩

## 상황
- 유니티 게임 클라이언트 개발자 취업/이직용 개인 포트폴리오 웹사이트를 만들고 있음
- 저장소: `AnsirH/ansirh.github.io` (GitHub Pages User Page, 현재 비어있음) → `https://ansirh.github.io`로 서빙 예정
- 최종적으로는 게임 3개(유니티 WebGL 빌드, 플레이 가능)와 최근 9개월간의 작업 5개(이미지+설명)를 각각 상세 페이지로 소개할 예정
- 이번 작업은 **실제 콘텐츠(이미지, WebGL 빌드) 없이 구조와 라우팅, 페이지 틀만** 잡는 것이 목표. 콘텐츠 채우기와 GitHub Actions 배포 설정은 다음 작업에서 별도로 진행함

## 기술 스택
- Astro (정적 사이트 생성기, 파일 기반 라우팅)
- Tailwind CSS (스타일링)
- Astro Content Collections (게임/작업 데이터 관리)

## 요구사항

### 1. 프로젝트 초기화
- `npm create astro@latest`로 Astro 프로젝트 생성 (Empty 템플릿)
- `astro add tailwind`로 Tailwind CSS 통합

### 2. 라우팅 구조
- `/` — 메인 페이지
- `/games/[slug]` — 게임 상세 페이지 (동적 라우트)
- `/work/[slug]` — 작업 상세 페이지 (동적 라우트)

### 3. Content Collections 스키마
- `games` 컬렉션: `title`, `description`(한 줄 요약), `slug`, `techStack`(배열), `playUrl`(WebGL 빌드 경로, 지금은 빈 값 허용) 등 필요한 필드로 스키마 정의
- `work` 컬렉션: `title`, `period`, `description`, `slug`, `images`(배열, 지금은 빈 배열 허용) 등 필요한 필드로 스키마 정의
- 각 컬렉션에 **더미 데이터 3개(게임), 5개(작업)**를 임시로 채워넣어서 라우팅과 레이아웃이 정상 동작하는지 확인 가능하게 할 것

### 4. 메인 페이지 (`/`)
- 히어로 섹션: 이름, 한 줄 소개, 연락처/링크 자리 (placeholder 텍스트로)
- 게임 3개 카드 그리드 (클릭 시 해당 게임 상세 페이지로 이동)
- 작업 5개 카드 그리드 (클릭 시 해당 작업 상세 페이지로 이동)

### 5. 상세 페이지 기본틀
- 게임 상세 페이지: 제목, 설명, 기술 스택, WebGL 임베드 자리(현재는 "빌드 준비 중" placeholder)
- 작업 상세 페이지: 제목, 기간, 설명, 이미지 자리(현재는 placeholder 이미지 또는 빈 박스)
- 두 페이지 모두 메인으로 돌아가는 네비게이션 포함

### 6. 공통 레이아웃
- 모든 페이지에서 재사용할 기본 `Layout.astro` (헤더/네비게이션, 푸터 포함)

## 이번 작업에서 제외할 것 (다음 단계에서 진행)
- 실제 게임/작업 콘텐츠 및 이미지 채우기
- Unity WebGL 빌드 파일 연동
- GitHub Actions 자동 배포 워크플로우
- 디자인 디테일(애니메이션, 폰트 커스터마이징 등) — 지금은 Tailwind 기본값 + 최소한의 레이아웃 정리 수준이면 충분

## 완료 기준
- `npm run dev`로 로컬 실행 시 메인 페이지에서 게임/작업 카드 클릭 → 각 상세 페이지로 정상 이동
- 더미 데이터 추가/수정 시 페이지 코드 변경 없이 콘텐츠만 반영되는 구조 확인

## 참고: 이후 이어질 작업
아래 작업들은 이번 범위에는 포함하지 않지만, 바로 다음 단계에서 진행될 예정이므로 구조를 짤 때 염두에 두면 좋음 (지금 미리 구현하라는 의미는 아님):
- **WebGL 연동**: `games` 컬렉션의 `playUrl` 필드와 게임 상세 페이지의 임베드 영역은 이후 `<iframe>`으로 실제 Unity WebGL 빌드를 붙일 예정. 빌드 파일은 `public/games/게임명/` 형태의 경로에 위치할 가능성이 높음
- **GitHub Actions 자동 배포**: 이후 `.github/workflows/`에 배포 워크플로우를 추가해 main 브랜치 push 시 자동 빌드/배포될 예정. 빌드 산출물 경로나 설정이 이 흐름과 크게 충돌하지 않도록 일반적인 Astro 관례를 따를 것

